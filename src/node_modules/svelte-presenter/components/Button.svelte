<script>
    import colors from '../action/color'

    export let active = false
    export let hsl = {}

</script>
<button use:colors={hsl} class="borders" class:active on:click><slot></slot></button>

<style>
    button {
		background-color: white;
		cursor: pointer;
		flex: 0;
		min-width: 5ch;
		padding: .25em;
	}

	button.active,
    button:active,
	button:focus,
	button:hover {
		background-color: hsla(var(--hue), var(--sat), var(--lum), 50%);
		outline: none;
	}
</style>