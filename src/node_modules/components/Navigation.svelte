<script>
	export let currentIndex
    export let pages = []
	export let setIndex = () => void 0
</script>

<nav>
	{#each pages as page, i}
		<div><button class:active={i === currentIndex} on:click={() => setIndex(i)}>{i+1}</button></div>
	{/each}
</nav>

<style>
    nav {
		display: flex;
		flex-direction: column;
		gap: .25rem;
    }
    div {        
		display: flex;
		flex: 0 0;
		flex-direction: column;
    }
    button {
		background-color: white;
		border: 2px solid hsl(0, 0%, 35%);
		border-radius: .5em;
		cursor: pointer;
		flex: 1 0;
		min-width: 5ch;
		padding: .25em;
	}

	button.active,
    button:active,
	button:focus,
	button:hover {
		background-color: hsla(0, 0%, 35%, 50%);
		outline: none;
	}
</style>